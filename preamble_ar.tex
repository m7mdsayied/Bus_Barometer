% =========================================================
% PREAMBLE (ARABIC): PACKAGES & DESIGN SETUP
% =========================================================

% 1. Page Layout
\usepackage[margin=0in]{geometry} 

% 2. Graphics and Colors
\usepackage{graphicx}
\usepackage{tikz}       % For precise absolute positioning
\usepackage{xcolor}

% 3. Language & Font Support (XeLaTeX)
\usepackage{fontspec}
\usepackage{polyglossia}

% Set Default Language to Arabic
\setmainlanguage{arabic}
\setotherlanguage{english}

% 4. Font Configuration
% We configure it to look in the local "fonts" folder
\setmainfont[
    Path = fonts/,
    UprightFont = Almarai-Regular.ttf,
    BoldFont = Almarai-Bold.ttf,
    Script = Arabic
]{Almarai}

\newfontfamily\arabicfont[
    Path = fonts/,
    UprightFont = Almarai-Regular.ttf,
    BoldFont = Almarai-Bold.ttf,
    Script = Arabic
]{Almarai}

% 5. Hyperlinks
\usepackage[hidelinks]{hyperref}

% 6. Graphics Path
\graphicspath{{images/static/}{images/charts_ar/}}

% 7. Custom Color Definitions (Same as English for Consistency)
\definecolor{ecesteal}{HTML}{005963}   % Dark Teal
\definecolor{ecestitle}{HTML}{00b5e2}  % Cyan
\definecolor{eceslink}{HTML}{0066cc}   % Blue
\definecolor{textgreen}{HTML}{4CAF50}
\definecolor{textpurple}{HTML}{C2185B}
\definecolor{textblue}{HTML}{2196F3}
\definecolor{textdark}{HTML}{333333}

% 8. Paragraph Formatting for Arabic
\setlength{\parindent}{0pt} % Often preferred in modern Arabic reports
\linespread{1.3}            % Arabic fonts usually need more line height


\usepackage{catchfile}  % For reading override files

\newcommand{\ECESContent}[2]{%
  \IfFileExists{overrides/#1.tex}{%
    \input{overrides/#1.tex}%
  }{%
    #2%
  }%
}

% 9. Automatic Backgrounds & Page Numbering
\usepackage{eso-pic}

% Create a variable to store the current background image filename
\def\CurrentBgImage{con_bg_ar.png} % Default background

% Create a switch to easily turn page numbers on/off (e.g., off for cover page)
\newif\ifShowPageNum
\ShowPageNumtrue

% Command to change the background image easily in your document
\newcommand{\SetBgImage}[1]{
    \def\CurrentBgImage{#1}
}

% Set up the universal background to automatically apply to EVERY page
\AddToShipoutPictureBG{%
    \begin{tikzpicture}[remember picture, overlay]
        % 1. Draw the Background Image dynamically
        \node[anchor=north west, inner sep=0pt] at (current page.north west) {
            \includegraphics[width=\paperwidth, height=\paperheight]{\CurrentBgImage}
        };

        % 2. Draw the Page Number (only if \ShowPageNum is true)
        \ifShowPageNum
            \node[anchor=south west] at ([xshift=1cm, yshift=1.2cm]current page.south west) {
                \fontsize{14}{14}\selectfont \textbf{\thepage}
            };
        \fi
    \end{tikzpicture}
}

% =========================================================
% LAYER 1 — DEFAULT TEXT COLOR
% =========================================================
\AtBeginDocument{\color{textdark}}

% =========================================================
% LAYER 1 — NAMED TIKZ BOX STYLES
% =========================================================
\tikzset{
  ECEShighlight/.style={%          exec summary highlights box
    draw=ecesteal, line width=1.5pt,
    fill=ecesteal!5,
    rounded corners=8pt,
    inner sep=12pt,
    align=justify,
    text width=\dimexpr\linewidth-24pt\relax
  },
  ECESattention/.style={%          priorities / green-accent box
    draw=textgreen, fill=textgreen!5,
    rounded corners=5pt,
    inner sep=10pt,
    text width=\dimexpr\linewidth-24pt\relax
  },
  ECESformula/.style={%            equation box (methodology)
    draw=ecesteal, fill=ecesteal,
    rounded corners=5pt,
    inner sep=14pt
  }
}

% =========================================================
% LAYER 2 — TYPOGRAPHY SEMANTIC COMMANDS (AR-specific sizes)
% =========================================================
\newcommand{\FontPageTitle}{\fontsize{24}{28}\selectfont}
\newcommand{\FontMacroTitle}{\fontsize{22}{26}\selectfont}
\newcommand{\FontBBITitle}{\fontsize{20}{24}\selectfont}
\newcommand{\FontReportSection}{\fontsize{18}{22}\selectfont}
\newcommand{\FontSectionHead}{\fontsize{13}{16}\selectfont}
\newcommand{\FontSubSection}{\fontsize{11}{14}\selectfont}
\newcommand{\FontBodyLg}{\fontsize{11}{15}\selectfont}
\newcommand{\FontBody}{\fontsize{10}{13}\selectfont}
\newcommand{\FontSmallHead}{\fontsize{9}{11}\selectfont}
\newcommand{\FontCaption}{\fontsize{10}{12}\selectfont}
\newcommand{\FontSource}{\fontsize{8}{10}\selectfont}
\newcommand{\FontNote}{\fontsize{7}{9}\selectfont}
\newcommand{\FontTableTitle}{\fontsize{9}{11}\selectfont}

% Compound structural helpers
\newcommand{\ECESSectionHeading}[2]{%
  {\FontSubSection\textbf{\color{#1}\underline{#2}}}}

\newcommand{\ECESPageSectionHead}[2]{%
  {\FontSectionHead\textbf{\color{#1}\underline{#2}}}}

\newcommand{\ECESPageTitle}[1]{%
  \noindent{\FontPageTitle\textbf{\color{ecestitle}#1}}}

\newcommand{\ECESFigCaption}[1]{%
  \noindent{\FontCaption\textbf{\color{ecestitle}#1}}}

\newcommand{\ECESSourceNote}[1]{%
  \noindent{\FontSource\textit{#1}}}

\newcommand{\ECESSmallNote}[1]{%
  \noindent{\FontNote\textit{#1}}}

\newcommand{\ECESFootnoteRule}{%
  \vfill\noindent\rule{5cm}{0.4pt}\vspace{0.3em}}

% =========================================================
% LAYER 3 — GEOMETRY PRESETS (AR: sidebar on the right)
% =========================================================
\newcommand{\GeomExecSummary}{\newgeometry{right=4.5cm, left=2cm, top=1cm, bottom=2.5cm}}
\newcommand{\GeomMacroMain}{\newgeometry{right=5cm, left=1.5cm, top=1cm, bottom=2.5cm}}
\newcommand{\GeomMacroDeep}{\newgeometry{right=5cm, left=1.5cm, top=3cm, bottom=2.5cm}}
\newcommand{\GeomStandard}{\newgeometry{left=2cm, right=2cm, top=3cm, bottom=2.5cm}}
\newcommand{\GeomTight}{\newgeometry{left=2cm, right=2cm, top=1cm, bottom=1cm}}
\newcommand{\GeomTightTop}{\newgeometry{left=2cm, right=2cm, top=1cm, bottom=2.5cm}}
\newcommand{\GeomMid}{\newgeometry{left=2cm, right=2cm, top=2cm, bottom=2.5cm}}